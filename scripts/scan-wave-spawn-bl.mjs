/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    —
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
import fs from 'node:fs';import path from 'node:path';import{fileURLToPath}from'node:url';const r=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../../..');const b=fs.readFileSync(path.join(r,'extraction-core/dumps/v29.0.0-arm64/libil2cpp.so'));const wu=0x01FCC87C,wue=0x01FCD064,t=0x01FDB514,s=[];for(let pc=wu&~3;pc+4<=wue;pc+=4){const w=b.readUInt32LE(pc);if((w>>26)!==0b100101)continue;let imm=w&0x3ffffff;if(imm&(1<<25))imm-=1<<26;if(pc+(imm<<2)===t)s.push(pc);}const o={source:'v29 BL scan',spawnRandomBasicEnemyCallCount:s.length,spawnRandomBasicEnemyCallSitesInWaveUpdate:s.map(x=>'0x'+x.toString(16)),rvas:{WaveUpdate:{start:wu,end:wue},SpawnRandomBasicEnemy:t}};const out=path.join(r,'packages/platform/src/game-data/v29.0/wave-spawn-disasm.json');fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,JSON.stringify(o,null,2));console.log(JSON.stringify(o,null,2));