/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    WORKSHOP_SECTION_DISCOUNT_MAX_PCT, WORKSHOP_SECTION_DISCOUNT_STEP, ENHANCEMENT_SECTION_DISCOUNT_MAX_PCT, ENHANCEMENT_SECTION_DISCOUNT_STEP, ENHANCEMENT_VAULT_DISCOUNT_MAX_PCT, ENHANCEMENT_VAULT_DISCOUNT_STEP, workshopLabLevelToSectionDiscountPercent, enhancementLabLevelToSectionDiscountPercent (+8 more)
 * depends    —
 * used by    src/data/workshop/index, src/internal/local-state/calculator-local-state-schemas, src/internal/shared-tool-inputs/from-research, src/internal/shared-tool-inputs/shared-tool-inputs, src/mechanics/calculators/run, src/mechanics/calculators/specs-generated
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export const WORKSHOP_SECTION_DISCOUNT_MAX_PCT = 49.5
export const WORKSHOP_SECTION_DISCOUNT_STEP = 0.5
export const ENHANCEMENT_SECTION_DISCOUNT_MAX_PCT = 30
export const ENHANCEMENT_SECTION_DISCOUNT_STEP = 0.3
export const ENHANCEMENT_VAULT_DISCOUNT_MAX_PCT = 25
export const ENHANCEMENT_VAULT_DISCOUNT_STEP = 2.5

function snapToDiscountStep(value: number, step: number, max: number): number {
  const clamped = Math.max(0, Math.min(max, value))
  const snapped = Math.round(clamped / step) * step
  return Math.round(snapped * 10) / 10
}

export function workshopLabLevelToSectionDiscountPercent(level: number): number {
  const clampedLevel = Math.max(0, Math.min(99, Math.floor(level)))
  const pct = clampedLevel * WORKSHOP_SECTION_DISCOUNT_STEP
  return snapToDiscountStep(pct, WORKSHOP_SECTION_DISCOUNT_STEP, WORKSHOP_SECTION_DISCOUNT_MAX_PCT)
}

export function enhancementLabLevelToSectionDiscountPercent(level: number): number {
  const clampedLevel = Math.max(0, Math.min(100, Math.floor(level)))
  const pct = clampedLevel * ENHANCEMENT_SECTION_DISCOUNT_STEP
  return snapToDiscountStep(pct, ENHANCEMENT_SECTION_DISCOUNT_STEP, ENHANCEMENT_SECTION_DISCOUNT_MAX_PCT)
}

export function enhancementLabLevelToVaultDiscountPercent(level: number): number {
  const clampedLevel = Math.max(0, Math.min(10, Math.floor(level)))
  const pct = clampedLevel * ENHANCEMENT_VAULT_DISCOUNT_STEP
  return snapToDiscountStep(pct, ENHANCEMENT_VAULT_DISCOUNT_STEP, ENHANCEMENT_VAULT_DISCOUNT_MAX_PCT)
}

function isLegacyLabLevelValue(raw: number, maxPercent: number): boolean {
  return raw > maxPercent && Number.isInteger(raw)
}

export function normalizeWorkshopSectionDiscountPercent(value: unknown, fallback = 0): number {
  const raw = Number(value)
  if (!Number.isFinite(raw)) return fallback
  if (isLegacyLabLevelValue(raw, WORKSHOP_SECTION_DISCOUNT_MAX_PCT)) {
    return workshopLabLevelToSectionDiscountPercent(raw)
  }
  return snapToDiscountStep(raw, WORKSHOP_SECTION_DISCOUNT_STEP, WORKSHOP_SECTION_DISCOUNT_MAX_PCT)
}

export function normalizeEnhancementSectionDiscountPercent(value: unknown, fallback = 0): number {
  const raw = Number(value)
  if (!Number.isFinite(raw)) return fallback
  if (isLegacyLabLevelValue(raw, ENHANCEMENT_SECTION_DISCOUNT_MAX_PCT)) {
    return enhancementLabLevelToSectionDiscountPercent(raw)
  }
  return snapToDiscountStep(raw, ENHANCEMENT_SECTION_DISCOUNT_STEP, ENHANCEMENT_SECTION_DISCOUNT_MAX_PCT)
}

export function normalizeEnhancementVaultDiscountPercent(value: unknown, fallback = 0): number {
  const raw = Number(value)
  if (!Number.isFinite(raw)) return fallback
  if (isLegacyLabLevelValue(raw, ENHANCEMENT_VAULT_DISCOUNT_MAX_PCT)) {
    return enhancementLabLevelToVaultDiscountPercent(raw)
  }
  return snapToDiscountStep(raw, ENHANCEMENT_VAULT_DISCOUNT_STEP, ENHANCEMENT_VAULT_DISCOUNT_MAX_PCT)
}

export function maxDefinedWorkshopSectionDiscountPercent(...values: Array<number | null | undefined>): number {
  let max = 0
  for (const value of values) {
    if (value == null || !Number.isFinite(value)) continue
    max = Math.max(max, normalizeWorkshopSectionDiscountPercent(value))
  }
  return max
}

export function maxDefinedEnhancementSectionDiscountPercent(...values: Array<number | null | undefined>): number {
  let max = 0
  for (const value of values) {
    if (value == null || !Number.isFinite(value)) continue
    max = Math.max(max, normalizeEnhancementSectionDiscountPercent(value))
  }
  return max
}

export function maxDefinedEnhancementVaultDiscountPercent(...values: Array<number | null | undefined>): number {
  let max = 0
  for (const value of values) {
    if (value == null || !Number.isFinite(value)) continue
    max = Math.max(max, normalizeEnhancementVaultDiscountPercent(value))
  }
  return max
}

export function normalizeSharedWorkshopDiscounts<T extends {
  discountAttack: number
  discountDefense: number
  discountUtility: number
  enhancementDiscountAttack: number
  enhancementDiscountDefense: number
  enhancementDiscountUtility: number
  enhancementDiscountVault: number
}>(value: T, fallback: T): T {
  return {
    discountAttack: normalizeWorkshopSectionDiscountPercent(value.discountAttack, fallback.discountAttack),
    discountDefense: normalizeWorkshopSectionDiscountPercent(value.discountDefense, fallback.discountDefense),
    discountUtility: normalizeWorkshopSectionDiscountPercent(value.discountUtility, fallback.discountUtility),
    enhancementDiscountAttack: normalizeEnhancementSectionDiscountPercent(
      value.enhancementDiscountAttack,
      fallback.enhancementDiscountAttack,
    ),
    enhancementDiscountDefense: normalizeEnhancementSectionDiscountPercent(
      value.enhancementDiscountDefense,
      fallback.enhancementDiscountDefense,
    ),
    enhancementDiscountUtility: normalizeEnhancementSectionDiscountPercent(
      value.enhancementDiscountUtility,
      fallback.enhancementDiscountUtility,
    ),
    enhancementDiscountVault: normalizeEnhancementVaultDiscountPercent(
      value.enhancementDiscountVault,
      fallback.enhancementDiscountVault,
    ),
  } as T
}
