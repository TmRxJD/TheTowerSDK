/**
 * thetowersdk — build tools for The Tower.
 *
 * This barrel re-exports the two browser-safe entry points:
 *
 *   `thetowersdk/data`  game tables — costs, levels, effects, catalogs
 *   `thetowersdk/save`  read a player's save file into typed values
 *
 * Prefer those subpaths in application code so bundlers can drop what you do not
 * use. The save decoder needs Node and is imported from `thetowersdk/node`.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    atIndex, hasOwnKey, ownLookup, ownLookupOr
 * depends    src/data, src/formatting, src/save, src/support/own-lookup
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './data'
export * from './save'

// Number formatting that matches how the game displays values ("1.23K", "4.5B").
export * from './formatting'

/*
 * Own-key lookups, public because the rule they enforce is not SDK-internal.
 *
 * Any consumer indexing a record with an id from a save, a sheet, a URL or a user is one
 * `record[key]` away from getting an `Object.prototype` member back — and `??`, `||` and a
 * truthiness check all fail to reject one. Sharing the helper is how the same convention
 * holds on both sides of the package boundary.
 */
export { atIndex, hasOwnKey, ownLookup, ownLookupOr } from './support/own-lookup'
