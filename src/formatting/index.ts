/**
 * Formatting values the way the game displays them.
 *
 * Number notation ("1.23K", "4.5B") and durations, plus the parsers that read
 * those strings back.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/formatting/duration, src/formatting/numbers, src/formatting/tools
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './numbers'
export * from './duration'
export * from './tools'
