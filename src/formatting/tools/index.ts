/**
 * Grouped and abbreviated forms for numbers shown beside each other.
 *
 * Separate from `numbers/` because these choose a presentation — a coin total, a shard count, a
 * multiplier — rather than answering what a number is.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/formatting/tools/compute
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './compute'
