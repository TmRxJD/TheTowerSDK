/**
 * Durations in the units a run is measured in.
 *
 * Runs are talked about in hours and minutes, and stored in whatever the source happened to use —
 * seconds from a save, a `5h58m6s` string from a battle report, a decimal count of hours from a
 * spreadsheet. Everything here converts between those and one written form.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/formatting/duration/compute
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './compute'
