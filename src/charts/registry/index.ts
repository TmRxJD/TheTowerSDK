/**
 * Which charts exist, and what each one is about.
 *
 * A chart definition is the catalog entry — its path, its renderer, and the columns it expects.
 * The two link tables say which calculator and which mechanic a chart belongs to, so a consumer
 * showing a calculator can offer the charts that explain it without hard-coding the pairing.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/charts/registry/calculator-links, src/charts/registry/data, src/charts/registry/mechanic-links
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './data'
export * from './calculator-links'
export * from './mechanic-links'
