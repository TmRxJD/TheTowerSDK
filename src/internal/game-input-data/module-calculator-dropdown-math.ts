/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    buildModuleDiscountEntries, formatModuleDiscountOptionLabel
 * depends    src/internal/game-input-data/types
 * used by    src/internal/game-input-data/dropdown-evaluator, src/internal/game-input-data/index, src/mechanics/calculators/specs-generated
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
import type { GameDropdownOptionEntry } from './types'

export function buildModuleDiscountEntries(): readonly GameDropdownOptionEntry[] {
  return Array.from({ length: 31 }, (_, index) => ({
    value: index,
    baseValue: index,
  }))
}

export function formatModuleDiscountOptionLabel(pct: number): string {
  return `${Math.max(0, Math.min(30, Math.floor(Number(pct) || 0)))}%`
}
