/**
 * What a perk is worth.
 *
 * The perk term is itself a nested formula — `(1 + 0.15 x quantity) x (1 + standard perk bonus)` —
 * rather than a single number, which is the thing most often collapsed into one and got wrong.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/mechanics/perks/benefit
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './benefit'
