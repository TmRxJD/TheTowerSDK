/**
 * Guardian damage and the chips that shape it.
 *
 * Small, and separate anyway: a guardian's tick damage is an input to coins per kill through the
 * Bounty chip, so a coin model with no guardian term is missing a factor rather than mis-sizing
 * one.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/mechanics/guardians/compute
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './compute'
