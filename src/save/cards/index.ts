/**
 * Card levels and mastery, indexed by slot.
 *
 * `cardLevel[i]` and `cardCount[i]` are positional against the card catalog, so the slot order is
 * load-bearing: a card inserted anywhere but the end shifts every entry after it.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/save/cards/read
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './read'
