/**
 * Bot unlocks, levels and spending, as a save stores them.
 *
 * Levels are POSITIONAL, and the four stats are not the same across bots — Flame Bot upgrades
 * Damage Reduction and Damage, Thunder Bot upgrades Linger, and only Golden, Amplify and Bot Bot
 * take Duration and Bonus. Reading one bot's array against another's stat order silently assigns
 * every level to the wrong stat.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/save/bots/read
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './read'
