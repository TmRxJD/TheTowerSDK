/**
 * Workshop levels, in both currencies.
 *
 * Coin levels persist and cash levels reset every battle. The same stat exists in both, they are
 * stored separately, and writing an in-round cash level where a coin level belongs claims progress
 * the player has not bought.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/save/workshop/read
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './read'
