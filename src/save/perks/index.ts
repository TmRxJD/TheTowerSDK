/**
 * Perk picks and the presets they are saved under.
 *
 * A save holds several presets and the banned list alongside them, so "the player's perks" is a
 * question about which preset rather than a single answer.
 */
/* ── file map ─────────────────────────────────────────────────────────────────
 * exports    —
 * depends    src/save/perks/read
 * used by    —
 * ─────────────────────────────────────────────── generated by `npm run map` ──
 */
export * from './read'
